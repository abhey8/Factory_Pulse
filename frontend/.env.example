# Frontend API base path.
# Keep "/api" when running Vite with the included proxy or when Express serves
# the built frontend. Use a full URL only if the frontend is hosted separately.
VITE_API_BASE_URL="/api"
